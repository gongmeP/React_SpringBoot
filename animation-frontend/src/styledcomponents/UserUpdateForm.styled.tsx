import { Col, Row } from 'react-bootstrap';
import styled from 'styled-components';

export const UserColStyled = styled(Col)`
  font-size: 1rem;
`;

export const UserRowStyled = styled(Row)`
  align-items: center;
  height: 60px;
`;
